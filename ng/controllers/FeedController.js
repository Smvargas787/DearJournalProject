DJournalApp.controller("sign-upController", function ($scope, $firebaseAuth, $firebaseArray) {
  // var ref = firebase.database().ref();
  // var avatarRef = ref.child('avatars');
  // var avatars = $firebaseArray(avatarRef);
});
