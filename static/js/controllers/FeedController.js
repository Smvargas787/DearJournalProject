DJournalApp.controller("sign-upController", ["$scope", "$firebaseAuth", "$firebaseArray", function ($scope, $firebaseAuth, $firebaseArray) {
  // var ref = firebase.database().ref();
  // var avatarRef = ref.child('avatars');
  // var avatars = $firebaseArray(avatarRef);
}]);
